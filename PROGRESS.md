# 進捗と運用手順

このファイルと `content-plan.json` が、コンテンツ拡張作業の**永続的な状態**です。
セッションが切れても、この2つを読めば続きから再開できます。

## 目標

| 項目 | 開始時 | 現在 | 目標 |
|---|---|---|---|
| クイズ | 980問 | (下記コマンドで確認) | 5,000問 |
| 座学モジュール | 36 | (下記) | 186 |
| 座学の記事 | 293 | (下記) | 約1,870 |

現状を数える:
```
node -e "eval(require('fs').readFileSync('js/terms-data.js','utf8'));console.log('クイズ',TERM_QUESTIONS.length,'問')"
grep -l 'study-section' study-*.html | xargs grep -c 'class=\"study-section\"' | awk -F: '{s+=$2}END{print \"モジュール\",s}'
grep -h '<article' study-*.html | wc -l
```

## 作業の単位

`content-plan.json` の `modules[]` が作業単位です。各要素:

- `key` / `prefix` — クイズIDのプレフィックス（`n001-` 〜 `n140-`）
- `page` — 挿入先のHTMLファイル
- `id` — セクションのid（`module-n001`）
- `title` / `level` / `category` / `articles` / `quizzes` / `topics`
- `status` — `pending` / `done`

## 1サイクルの手順

1. `content-plan.json` から `status: "pending"` のモジュールを最大18件取り出す
   （同時実行の上限が20のため）
2. 各モジュールにつきエージェントを1つ起動する。指示は
   `scratchpad/w/BRIEF.md` を読ませたうえで、モジュール仕様と出力先を渡すだけでよい
3. 出力は `scratchpad/w/<key>.html` と `scratchpad/w/<key>.json`
4. 全部揃ったら `node tools/build-wave.js` で検証・挿入・マージ
   （1つでも基準を外れたら書き込まずに中断する）
5. 検証が通ったらコミットして push、`main` へ fast-forward マージ
6. `content-plan.json` の該当モジュールを `status: "done"` に更新してコミット
7. 目標に届くまで1へ戻る

## クイズの品質基準（機械検査）

`content-plan.json` の `quizRules` が正。要点:

- 選択肢4つ、すべて相異、文字数の最長と最短の差が最長の30%以内
- **「最長戦略」の期待正答率20〜32%**（同点はランダム選択として1/同点数で計上）
- **「最短戦略」の期待正答率15%以上**（逆方向の偏りを防ぐ）
- 正解位置が0〜3にほぼ均等
- **解説が誤答3つすべてに言及している**
- 解説が選択肢を位置番号で参照していない
- 既存問題との実質重複がない
  （問題文の類似度0.85以上、または 問題文0.60以上かつ正解0.50以上）

### 過去に踏んだ落とし穴（同じ失敗を繰り返さないこと）

1. **誤答3つの平均と正解を比べても偏りは見つからない。**「4つのうち正解が単独最長か」で
   見ること。さらに同点を数えないと見落とす。同点はランダム選択として計上する。
2. **重複判定をAND条件にすると、問題文が完全一致でも正解の表現が違えば通り抜ける。**
   「問題文がほぼ同一ならそれだけで重複」というOR条件を必ず入れる。
3. **解説の選択肢参照は0始まりと1始まりが混在しうる。**位置番号で参照させないのが唯一の解。
4. **エージェントの自己申告は必ず実測で裏取りする。**「検査済み」という報告が
   実測と食い違った事例が多数ある。

## ラーニングジャーニー(ステップ学習)のUI — 2026-08-31 追加

`js/journey.js` が全 study-*.html を「モジュール一覧(ジャーニーマップ) →
記事を1本ずつ → そのモジュールの確認クイズ → 次のモジュール」というステップ式に
変換する。進捗は localStorage の `journey_v1`、クイズ回答は quiz.html と共通の
通算カウンター(`toshipro5000_progress_v2`)にも加算される。

**コンテンツ追加時の注意:**

- `module-n\d\d\d` 形式のidを持つ新モジュールは、クイズIDのプレフィックス一致で
  **自動的に**ジャーニーへ組み込まれる。作業は不要。
- 旧来形式のid(`module-xxx`)や、座学に紐づかない新カテゴリのクイズを足した場合は、
  `js/journey.js` の `LEGACY_MAP` / `DRILLS` に対応を追記すること。
- 検証: `node tools/test-journey.js`(要 jsdom。全ユニットが1問以上に対応し、
  全4,140問がいずれかのユニットから解けることを確認する)。
- 長い記事(1,400字超)は表示時にブロック単位で約900字のステップへ自動分割される
  (`js/journey.js` の `splitLongArticles`)。HTML自体は分割しないので、
  記事を書くときに長さを気にする必要はない。

## 座学の用語解説と図解 — 2026-08-31 追加

**記事下の用語解説(自動)**: `js/term-notes.js` が、座学の各記事に出てきた
用語集(glossary.html)の用語を検出し、記事の下に「📖 この記事に出てきた用語」
ボックス(名称・一行定義・用語集へのリンク)を自動表示する。
同じモジュール内では最初に出てきた記事の下にだけ載せる。

- データは `js/glossary-data.js`(自動生成)。**glossary.html に用語を
  追加・修正したら `node tools/build-glossary-data.js` で再生成してコミット**すること。
- 表記ゆれ(例: ヴィンテージ/ビンテージ)は同ツール内の `EXTRA_ALIASES` に追記する。
- 読み込み順は `glossary-data.js` → `term-notes.js` → `journey.js`(全 study-*.html)。
  ステップ学習の記事分割より前にDOMへ挿入する必要があるため、この順を崩さないこと。

**図解・表・グラフ**: `style.css` に共通コンポーネントを追加した。
新しい座学記事にも積極的に使うこと。

- `<figure class="study-figure">` + インラインSVG + `<figcaption>`。
  SVGは `viewBox` 必須、色は必ずCSS変数(`var(--accent)` 等)と
  `currentColor` / `class="fig-muted"` / `class="fig-line"` を使う
  (ダークテーマ対応のため色コード直書き禁止)。横長のSVGは
  `<div class="fig-scroll">` で包む(スマホでは横スクロールになる)。
  SVG内の `<marker>` 等の id はページ内で一意にすること。
- 表は `<div class="table-scroll"><table class="study-table">`。
  数値セルは `class="num"`。
- 挿入位置は必ず `<article>` の内側(ステップ学習で記事と一緒に移動させるため)。
- 現在の設置箇所: 初級9(GP/LP構造図・ライフサイクル・キャリア表・財務3表・
  EVブリッジ・S&U表・レバレッジ比較グラフ・資本構成図・ミニLBOフロー図)、
  中級5(DD一覧表・ストラクチャー階層図・ステーク/アセット表・トランシェ表・
  コベナンツ比較表)、上級4(ウォーターフォール図・欧州/米国型表・
  トランシェ表・Exit比較表)。専門・営業ほか他ページはまだ図解なし(今後の課題)。

## 品質バーの引き上げ — 2026-09-03(重要・以後の全執筆に適用)

利用者から「クイズも座学も雑。用語クイズレベル。初級でLBOモデルを感応度含め完全に
作れる内容になっていない。ビジネスDD・契約・ストラクチャー・ローンも網羅と掘り下げが
足りず、実務と紐付いていない」という趣旨の指摘を受けた。これへの対応として:

- **執筆基準を `tools/BRIEF.md` に固定した。** 以後のモジュール執筆(波の再開を含む)は
  必ずこのブリーフを読ませること。要点: 記事は実務タスク単位(誰が・どの局面で・何を
  作って提出するか)/定義の羅列禁止/検算可能な数値例・条項サンプル文言・チェックリスト
  必須/モジュールごとに固有の通し案件(既出の社名・EBITDA・倍率と重複させない)/
  クイズは全問シナリオ・計算・エラー発見型で定義問題禁止。
- **単体検証ツール `tools/check-module.js` を追加した。**
  `node tools/check-module.js <key> --dir <出力ディレクトリ>` で build-wave と同じ基準を
  1モジュール単位で検査できる。執筆エージェントに自己検証させてから build-wave に
  かけると、一発合格率が大きく上がる(2026-09-03の波は21/21が一発合格)。
- **LBOはレベル設計を作り直した。** 初級=`study-beginner.html` モジュール12
  (module-n153「LBOモデル完全構築」):白紙のブックからS&U・P/L・デットスケジュール・
  キャッシュスイープ・3表接続・リターン・感応度2種・卒業課題まで、検算値つきで
  一人で組み切れる内容。中級=`study-intermediate.html` モジュール12
  (module-n154「LBOモデルのレビューと応用」):レビューの型・平均残高と循環参照・
  スイープ率/RCF/PIK・コベナンツヘッドルーム・バリューブリッジ・感応度設計・
  リキャップ/アドオン/対価スキームの応用・モデル診断。両モジュールの数値は
  同一のT社案件(EBITDA20億・7.0倍・TLA50+TLB40・エクイティ60、MOIC2.87倍・IRR23.5%)で
  貫通しており、スクリプトで検算済み。今後この2本の数値に触れる場合は整合を崩さないこと。
- **初級モジュール13(module-n155「LBOモデル実装ブループリント」)を追加(2026-09-03追記)。**
  「座学だけでは実際に組めない」という指摘への対応で、①売上ドライバーからのP/L
  (売上200+10/年×粗利率40%−販管費62+2/年→EBITDA22〜30に厳密整合)、
  ②のれん80億円(対価110−純資産簿価30)を含む開始B/S(資本53=出資60−フィー7)、
  ③行番号・セル番地・数式レベルのCalcシート行マップ(2〜58行)、④CF計算書3区分、
  ⑤5年分のB/S完全ロール検収表(チェック行は恒等的に0.000)、⑥詰まりどころFAQ10選
  を収録。T社の数値体系はモジュール12と完全に貫通(バリューブリッジの「フィー7億円」が
  開始B/Sの資本53億円として回収される構造)。この数値体系に触れる場合は3モジュール
  (初級12・13、中級12)全部の整合を確認すること。

### 2026-09-03 に完了した波

n060〜n062(DD各論3本)、n077〜n092(ストラクチャー8本・契約実務8本)、n153・n154・n155
(LBO初級/中級/実装編)の計22モジュール・記事223本・クイズ448問を取り込み済み
(4,140→4,588問)。旧「波6の中断」は解消。ジャーニーテスト(test-journey.js)は
期待ユニット数を22→24に更新して ALL PASS。

## 未完了の作業（2026-09-03 時点）

### 1. 残りの計画モジュール(48本)
`content-plan.json` の pending は n093〜n140 の48本(交渉2・バリューアップ14・Exit8・
セクター12・横断12)。再開時は `tools/BRIEF.md` を各エージェントに読ませ、
`tools/check-module.js` で自己検証させてから `tools/build-wave.js` で取り込むこと。

### 2. カテゴリ・レベル単位の文字数バランスが未対応(継続)
`tools/check-bias.js` を追加した。全体では 最長25.0%/最短20.0% と
基準内だが、**出題時に実際に絞れる単位で見ると偏っている**。

- 上級 402問: 最短戦略 10.1%（合格は15%以上）
- 30問以上のカテゴリ36件中13件が基準外
  - 交渉・ステークホルダー実務 40問: 最長35.8%/最短1.3%
  - ファンド運営・LP対応 40問: 最長21.3%/最短0.8%
  - 投資形態 40問: 最短5.6%

「一番短い選択肢は答えではない」が読めると4択が実質3択になる。
`node tools/check-bias.js --min 30` で現状を確認できる。
修正は tools/export-explains.js と同じ要領で、対象カテゴリの問題を
書き出して誤答の長さを調整させる。

### 3. 解説の改稿の残り(継続)
誤答3つすべてに理由が書かれている問題は 4,089 / 4,588 (89.1%)。新規448問は全問対応済み。
残りは `node tools/export-explains.js <prefix> --out <dir>` で抽出できる。
